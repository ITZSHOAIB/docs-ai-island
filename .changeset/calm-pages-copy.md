---
"docs-ai-island": minor
---

Add explicit custom and URL-backed Content Sources, lazy `copyPage()` with exact Markdown/text clipboard handoff and opt-in canonical-URL fallback, and safe `viewMarkdown()` navigation resolved from current Page Context.
